import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { boolean, color, text, select } from "@storybook/addon-knobs";
import { iconPlacementOptions } from "./search.types.ts";
import { sizes, colors } from "../TextInput/textInput.types.ts";
import { fontSizes } from "../../../types/typography";
import Search from "./Search.tsx";

<Meta title="Search" component={Search} />

<Preview>
    <Story name="default">
        <Search
            iconPlacement={select(
                "icon placement",
                iconPlacementOptions,
                "left",
                ""
            )}
            fullWidth={boolean("full width", false)}
            size={select("size", sizes, "medium", "")}
            color={select("color", colors, "medium", "")}
            iconSize={select("icon size", Object.keys(fontSizes), "small", "")}
            placeholder={text("placeholder", "type something...")}
            round={boolean("round", false)}
        />
    </Story>
</Preview>

## Props

<Props of={Search} />
