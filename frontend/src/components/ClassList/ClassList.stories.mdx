import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { boolean, color, text, select } from "@storybook/addon-knobs";
import ClassList from "./ClassList.tsx";

<Meta title="ClassList" component={ClassList} />

<Preview>
    <Story name="default">
        <ClassList
            showDetails={boolean("show details", false)}
        />
    </Story>
</Preview>

## Props

<Props of={ClassList} />
